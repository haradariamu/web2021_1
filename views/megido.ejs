<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>メギド一覧</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.スタイル %></td>
            <td><%= row.クラス %></td>
            <td><%= row.都道府県 %></td>
            <td><%= row.人口 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>