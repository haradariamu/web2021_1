<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>レース詳細</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name %></td>
            <td><%= row.course %></td>
            <td><%= row.kyori %></td>
        </tr>
        <img src="keiba.<%= row.name %>">
      <% } %>
  </table>
</body>
</html>